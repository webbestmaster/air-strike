/*global PIXI, define */
define({

	XY_DIVIDER: ',',
	PUSH_OBJECT: 'collision-manager:push-object',
	REMOVE_OBJECT: 'collision-manager:remove-object',
	DESTROY_OBJECT: 'collision-manager:destroy-object'

});
